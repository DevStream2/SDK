// build.gradle à la racine

plugins {
    // Pas de plugins ici si les modules les gèrent séparément
}

allprojects {
    repositories {
        mavenCentral()
        // Autres repos si besoin
    }
}
